# Copiar a .env (local) o configurar en Hostinger → Variables de entorno
SESSION_SECRET=cambiar_por_un_secreto_largo

# En Hostinger: rutas ABSOLUTAS a carpetas PERSISTENTES (fuera del clon de deploy)
# DATA_DIR=/home/uXXXX/domains/tudominio.com/nodejs/data
# UPLOADS_DIR=/home/uXXXX/domains/tudominio.com/nodejs/uploads

# No definir PORT en producción (Hostinger lo asigna)
